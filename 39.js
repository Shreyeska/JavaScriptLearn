//39. Create an object representing a car with properties: make, model, and year.

const car = {
    make:'BMW',
    model: 'i5 Sedan',
    year: 2024
};

console.log(`Name: ${car.make}`);
console.log(`Model: ${car.model}`);
console.log(`Year: ${car.year}`);
