// 2. Function that takes a string as input and returns its length.

function strlen (str){
    return str.length;
}

const input = 'This is JavaScript test for length';
const len = strlen(input);
console.log(len); // output = 34